/**
 * 常量
 */
// const rootURL = `${location.protocol}//${location.hostname}:${location.port}/api/v1`;
const rootURL = "http://44.238.134.212:8020/api/v1";

// abi管理页的URL前缀
export const abiUrlPrefix = rootURL + "/abi";
// address管理页的URL前缀
export const addrUrlPrefix = rootURL + "/addr";









